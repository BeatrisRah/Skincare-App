import { View } from "react-native";
import TemplateSection from "./TempSection";

export default function MorningSection() {
    return (
       <View>
            <TemplateSection steps={{}} type={"Morning" } />
       </View>
    );
}